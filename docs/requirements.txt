# Documentation build dependencies (OasisPlatform docs/source)
# The REST API reference is rendered by Redoc, vendored as a standalone bundle in
# _static/redoc/ — no Sphinx extension, so no Sphinx version cap or pkg_resources pin.
sphinx
furo
myst-parser
sphinx-design
sphinx-copybutton
